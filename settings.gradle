pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
rootProject.name = "Sprites"
include 'app', 'util', 'sprites:javafx', 'sprites:model'
